//1. 변수
let age;

age = 30;

//2. 상수(초기 이후 값 못 바꿈)
const birth = "2004.07.05";

//3.변수 명명규칙(네이밍 규칙)
//3-1. $,_제외한 기호는 사용할 수 없다!
let $_name;

//3-2. 변수의 이름은 숫자로 시작할 수 없다.
let name1;

//3-3. 예약어(let같은것들)를 사용할 수 없다.

//4. 변수 명명 가이드
let a = 1;
let b = 1;
let c = a - b;
//동료와 협업시 이름구려서 별로임.
let salesCount = 1;
let refundCount = 1;
let totalSalesCount = salesCount - refundCount;
//위에처럼 명확한 명칭으로 변수명 설정하기
